{
	"name": "archive",
	"description": "TEXTaiLES Digital Archive - Public 3D artefact viewer",
	"icon": "extension",
	"version": "1.0.0",
	"keywords": [
		"directus",
		"directus-extension",
		"directus-extension-endpoint"
	],
	"files": [
		"dist"
	],
	"directus:extension": {
		"type": "endpoint",
		"_comment": "path must stay 'index.js': Directus 9 hardcodes entrypoint 'index.js' for local (non-npm) extensions and ignores this field, so building anywhere else produces a file it never reads.",
		"path": "index.js",
		"source": "src/index.js",
		"host": "^9.22.0",
		"id": "archive"
	},
	"scripts": {
		"build": "directus-extension build",
		"dev": "directus-extension build -w --no-minify",
		"link": "directus-extension link",
		"validate": "directus-extension validate"
	},
	"devDependencies": {
		"@directus/extensions-sdk": "9.22.0"
	},
	"dependencies": {
		"build": "^0.1.4",
		"obj2gltf": "^3.1.4"
	}
}
